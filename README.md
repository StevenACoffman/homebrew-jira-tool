# Install [jt](http://github.com/StevenACoffman/jt) jira tool cli with brew

It is that simple :

```shell
brew tap StevenACoffman/jt
brew install jt
```

Release should get picked up automatically with `brew upgrade`
