# Install `jt` - [jira-tool](http://github.com/StevenACoffman/jira-tool) cli with brew

It is that simple :

```shell
brew tap StevenACoffman/jira-tool
brew install jira-tool

```

Release should get picked up automatically with `brew upgrade`
